# from .base import Base
# from sqlalchemy import Column, ForeignKey, String
# import uuid

# class UserStoryDomain(Base):
#     __tablename__ = 'user_story_domains'

#     domain_id = Column(String(36), ForeignKey('domains.id'), primary_key=True)
#     user_story_id = Column(String(36), ForeignKey('user_stories.id'), primary_key=True)

